📄 דוח בילד – גרסת Kaggle Hybrid (תקציר בעברית)
🐳 מה קורה בזמן docker compose build?

בשלב הבילד מתבצע תהליך מלא ודטרמיניסטי:

התקנת תלויות (requirements.txt)

פיצול דאטה אמיתי (47,837 טיקטים מ-Kaggle) ל-Train/Val/Test בצורה סטרטיפיקטיבית

הרחבת Train בלבד באמצעות EDA (ללא Data Leakage)

אימון מודל TF-IDF + Logistic Regression

אינג’סט של מסמכי Runbooks ל-Chroma (RAG)

בסיום הבילד הקונטיינר כולל:

מודלים מאומנים

Vector Store מוכן

סביבת ריצה מלאה

⏱ זמן בילד ראשוני: ~2 דקות
⏱ בילד חוזר (cache): מספר שניות

🚀 מה קורה בזמן docker compose up?

הקונטיינר עולה

FastAPI נטען

המודלים נטענים לזיכרון

שרת מאזין לפורט 8000

⏱ זמן עלייה: פחות משנייה
אין אימון או אינג’סט בזמן ריצה.

🎯 למה אימון בזמן Build?

✔ עלייה מיידית בדמו
✔ זיהוי כשלים מוקדם
✔ תוצאה ניתנת לשחזור
✔ אין עיכוב בזמן ריצה

✅ בדיקות לפני דמו

docker compose up --build מסתיים בהצלחה

/health מחזיר תקין

/triage מחזיר JSON תקין

19 טסטים עוברים